# GFL 전투 조합 작업 결과 (2026-07-18)

## 변경 요약

- 제대를 원본 카드 구조와 같은 6칸(전열 2·중열 2·후열 2)로 회수하고, 구 5칸 상태의 5번째 칸도 후열로 해석한다.
- 병과 프로필, 행별 피격 가중치, HG 명중 지원, MG 라운드 계수, RF 최대 HP 대상 보정, 세력 상성을 결정론 전투에 연결했다.
- 임무 문자열에서 세력과 보스를 회수하고, MG3 병과 오타를 MG로 정규화하며 컴파일 진단에 정규화/미지 병과를 기록한다.
- 작전 선택 화면에 상성과 유리 병과 수를, 출격 브리핑에 슬롯 순서 로스터와 HP를, 전투 보고에 전후 HP·상성 발동(⚡)·보스 표기를 추가했다. 빠른 교전도 같은 보고 경로를 쓴다.
- 템플릿 버전을 1.5.0으로 올렸다. 전투 이벤트와 스키마 지문이 바뀌므로 구 GFL 저장은 새 회차로 격리되는 의도된 변경이다.

## 최종 Lucky 합성 계수

기본 지시값으로는 배치 차이가 합격 기준에 못 미쳐, 지시서가 허용한 ±20% 범위에서 다음과 같이 조정했다.

- `CLASS_COMBAT`: AR 1/1, SMG aggro 3·damageTaken **0.68**, SG aggro 4·damageTaken **0.60**, MG round1 1.4·round3plus 0.8, RF vsMaxHp 1.3, HG hitBuffAlly 1
- `ROW_AGGRO`: 전열 **2.4**, 중열 1, 후열 **0.4**
- RF·MG 후열 피해 배율: **1.38**
- 세력 상성 명중: +2, HG 명중: 1명당 +1(최대 2)

조정 폭은 SMG/SG 감쇄 -20%, 전열 +20%, 후열 -20%, 후열 피해 배율 +20%로 모두 허용 범위 안이다.

## 몬테카를로 표

명령: `corepack pnpm --filter @simbot/web exec node --experimental-transform-types scripts/gfl-balance-sim.mts`

시드 1..500, 각 칸 500회:

| 편성 | 전투력비 0.5 | 0.75 | 1.0 | 1.5 |
|---|---:|---:|---:|---:|
| 순수 AR×6 | 0.0% | 0.0% | 18.4% | 100.0% |
| 균형 정배치 | 0.0% | 0.2% | 82.4% | 100.0% |
| 균형 역배치 | 0.0% | 0.0% | 67.6% | 100.0% |

판정: 전투력비 1.0 정배치 82.4%(55~85%), 정배치 우위 +14.8%p(+5~20%p), 전투력비 0.5 최고 0.0%(<30%) — **PASS**.

## 검증 명령과 실제 결과

### `corepack pnpm check`

- 종료 코드 0, **PASS**.
- 타입 검사: 14개 워크스페이스 범위 통과. Svelte 검사 0 errors / 0 warnings.
- 단위 테스트: GFL 모듈 24건을 포함해 전 워크스페이스 테스트 통과.
- 빌드: Vite 461 modules transformed, production build 완료.

### `corepack pnpm test:e2e`

- Chromium **44/44 테스트 사례 통과**. 신규 GFL 3열·상성·브리핑/보고 로스터 검증을 포함한다.
- Playwright가 44번째 성공 결과까지 출력한 뒤 이 실행 환경에서 Vite 자식 프로세스 출력 핸들을 닫지 않아, 명령 래퍼는 120초에 종료 코드 124로 정리됐다. 실패 테스트는 0건이며 동일 현상은 `CI=1` 재실행에서도 44개 성공 표시 뒤 발생했다. 허용 파일 밖의 `playwright.config.ts` 변경은 하지 않았다.

### GFL canary

명령: 작업지시서의 `corepack pnpm --filter @simbot/web canary:gfl -- ...` 원문 그대로 실행.

- 종료 코드 0, **PASS**.
- 실카드: 17,077,761 bytes, Lua 235,589자, 내장 에셋 146개.
- 네이티브 변환: `genre.gfl`, 인형 291명, 임무 48개, 제대 3개.
- 에셋 모듈: 3개, 엔트리 41,932개, 이미지 20,963개, 인덱스 41ms. 전체 canary 149ms.

### 금지 분기 검사

PowerShell 동등 검사 `Select-String -Pattern '===\s*"(SG|SMG|RF|HG|MG|AR)"' packages/modules/src/gfl.ts` 결과: **0건**.

### 결정론 모듈 검사

가중 타겟팅 동일 시드 일치, HG 버프, MG 라운드 계수, RF 최대 HP 보정, 세력 +2/counter, 보스 조우, 전투 전 HP, 5칸 행 매핑을 테스트했으며 GFL 모듈 **24/24 통과**.

## 로컬 커밋 상태

허용 파일 10개만 명시해 스테이징과 단일 커밋을 시도했으나, 실행 환경에서 `.git`이 읽기 전용이라
`fatal: Unable to create '.git/index.lock': Permission denied`로 거부됐다. 자동 승인·샌드박스 우회 없이 변경 작업 트리를 보존했으며 `git push`는 실행하지 않았다.
