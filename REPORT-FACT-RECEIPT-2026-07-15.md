# 전투·소모품·상거래 결과 영수증 구현 보고서 (2026-07-15)

## 사용자에게 보이는 변화

- 전투 시작 시 상대 이름과 수가 `전투 개시 · 고블린 외 2`처럼 표시된다.
- 공격과 기술은 대상, 명중·치명타·빗나감, 피해, 적의 남은 HP, 적 전멸 여부를 표시한다.
- 적 턴은 명중 횟수와 총 피해, 플레이어의 남은 HP, 쓰러짐 여부를 표시한다.
- 전투 종료는 승리·도주·패배·종료 결과와 경험치, 골드, 레벨 업, 부활 HP를 표시한다.
- 소모품은 사용한 아이템과 회복 대상, 증가량, 회복 후 수치, 남은 수량을 표시한다.
- 판매·구매·일괄 구매·시설 증축·의뢰·보상은 품목과 수량, 골드 증감, 재고·보유량·결과를 표시한다.
- 여관 영업, 사건 선택, 숙박 문의 수락·거절, 우편 결과도 실제 로그에 있는 매출·건수·결과로 표시한다.
- 전투나 아이템 사용이 거부되면 `전투 수치 직접 지정 불가`, `재고 없음`, `이미 최대치`, `MP 부족`처럼 짧은 한국어 사유가 표시된다.
- 새 아이콘을 추가하지 않고 기존 아이콘만 재사용했다.

## 변경 파일

- `apps/web/src/player/FactReceipt.svelte`
- `apps/web/src/player/fact-receipt.test.ts`
- `apps/web/e2e/combat-console.spec.ts`
- `apps/web/e2e/inn-commerce.spec.ts`
- `REPORT-FACT-RECEIPT-2026-07-15.md`

`packages/modules` 아래 엔진 코드, 사건 시그니처, 로그 모양은 변경하지 않았다. 기존 영수증 케이스의 출력 문구도 변경하지 않았다.

## 지시서와 달리한 것

- 기준 커밋에 이미 관례에 맞는 `apps/web/src/player/fact-receipt.test.ts`가 존재했다. 같은 목적의 테스트 파일을 중복 신설하지 않고 기존 파일에 필드 단언 테스트를 확장했다.
- 그 밖에 지시서와 달리한 내용은 없다.

## 검증 결과

- `pnpm --filter @simbot/web test`: 22개 테스트 파일, 92개 테스트 통과. 이 중 사실 영수증 테스트는 27개다.
- `pnpm --filter @simbot/web typecheck`: 512개 파일, 오류 0건, 경고 0건.
- 수정 e2e 선별 실행: `combat-console.spec.ts`, `inn-commerce.spec.ts` 2개 통과.
- 루트 `pnpm verify`: 통과.
  - 전체 타입 검사: 14개 워크스페이스 범위 통과.
  - 전체 단위 테스트: 96개 테스트 파일, 434개 테스트 통과.
  - 프로덕션 웹 빌드: 통과.
  - e2e: 기존 workers 1 설정으로 33개 전부 통과.

검증 과정에서는 이 실행 환경의 PATH에 `pnpm`이 없어 Corepack 임시 shim을 사용했고, Playwright의 기존 workers 1 설정과 테스트 구성은 변경하지 않았다.
