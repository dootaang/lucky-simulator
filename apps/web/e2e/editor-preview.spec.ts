import{expect,test}from'@playwright/test';
const card=Buffer.from(JSON.stringify({spec:'chara_card_v3',spec_version:'3.0',data:{name:'미리보기 카드',description:'긴 상태 정보가 있는 시뮬레이션',first_mes:'미리보기 시작',mes_example:'',personality:'',scenario:'',creator_notes:'',system_prompt:'',post_history_instructions:'',alternate_greetings:[],tags:[],creator:'test',character_version:'1',extensions:{},group_only_greetings:[],character_book:{entries:[]},assets:[]}}));
test('봇 편집 시뮬레이션은 한 화면 제목과 한 열 미리보기를 유지한다',async({page})=>{await page.setViewportSize({width:1280,height:800});await page.goto('/');await page.locator('input[accept=".simpack,.charx,.png,.json"]').setInputFiles({name:'preview.json',mimeType:'application/json',buffer:card});await expect(page.getByText('미리보기 시작')).toBeVisible();await page.getByRole('button',{name:'봇 편집'}).click();const editor=page.getByLabel('봇 편집기');await expect(editor).toBeVisible();await editor.getByRole('button',{name:'시뮬레이션',exact:true}).click();const preview=editor.locator('.preview'),renderer=preview.locator('.renderer');await expect(renderer.getByRole('heading',{name:'플레이',exact:true})).toHaveCount(1);await expect(renderer.getByRole('navigation',{name:'프로젝트 화면'})).toHaveCount(0);const layout=await preview.evaluate(element=>{const screen=element.querySelector<HTMLElement>('.screen');return{fits:element.scrollWidth<=element.clientWidth+1,columns:screen?getComputedStyle(screen).gridTemplateColumns.split(' ').filter(Boolean).length:0};});expect(layout).toEqual({fits:true,columns:1});});
