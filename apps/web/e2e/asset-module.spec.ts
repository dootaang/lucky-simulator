import{expect,test}from'@playwright/test';import{zipSync}from'fflate';
test('별도 에셋 모듈을 카드에 연결하고 재시작 뒤 필요한 이미지만 표시한다',async({page})=>{await page.goto('/');await expect(page.getByRole('button',{name:'카드 가져오기',exact:true})).toBeVisible();const card=Buffer.from(JSON.stringify({spec:'chara_card_v3',spec_version:'3.0',data:{name:'모듈 카드',description:'별도 에셋 테스트',first_mes:'모듈 이미지 {{img::silvia_smile}}',mes_example:'',personality:'',scenario:'',creator_notes:'',system_prompt:'',post_history_instructions:'',alternate_greetings:[],tags:[],creator:'test',character_version:'1',extensions:{},group_only_greetings:[],character_book:{entries:[]},assets:[]}}));await page.locator('input[accept=".simpack,.charx,.png,.json"]').setInputFiles({name:'module-card.json',mimeType:'application/json',buffer:card});await expect(page.getByRole('button',{name:'에셋 모듈 연결'})).toBeVisible();const module=Buffer.from(zipSync({'assets/other/image/silvia_smile.webp':Uint8Array.of(82,73,70,70,4,0,0,0,87,69,66,80)}));await page.locator('input[accept=".charx,.zip"]').setInputFiles({name:'sprites.charx',mimeType:'application/zip',buffer:module});await expect(page.getByTitle('1개 이미지')).toContainText('sprites.charx');await expect(page.locator('.message .text img')).toHaveAttribute('src',/^blob:/);await page.reload();await expect(page.getByTitle('1개 이미지')).toContainText('sprites.charx');await expect(page.locator('.message .text img')).toHaveAttribute('src',/^blob:/);});
