import { describe,expect,it } from 'vitest'; import { createSimPack,packSimPack,sha256Hex,unpackSimPack } from '../src/index.ts';
describe('SimPack',()=>{it('standard SHA-256',()=>expect(sha256Hex(new TextEncoder().encode('abc'))).toBe('ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad'));it('roundtrips source and runtime',()=>{const source=new TextEncoder().encode('{"card":1}'),project=createSimPack({id:'test',title:'테스트',fileName:'card.json',sourceBytes:source,schema:{meta:{id:'test'},initialState:{day:1}},screens:[{id:'play',title:'대화',layout:'chat',regions:{}}]});const restored=unpackSimPack(packSimPack(project));expect(restored.manifest.title).toBe('테스트');const ref=((restored.manifest.source as Record<string,unknown>).card as Record<string,unknown>).blob as{path:string};expect(restored.files[ref.path]).toEqual(source);});it('rejects path traversal',()=>{const project=createSimPack({schema:{meta:{id:'x'}}});project.files['../evil']=Uint8Array.of(1);expect(()=>packSimPack(project)).toThrow(/unsafe_path/);});});
