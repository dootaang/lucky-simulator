import { createRequire } from 'node:module'; import { describe,expect,it } from 'vitest'; import { createSimPack,packSimPack,unpackSimPack } from '../src/index.ts';
const require=createRequire(import.meta.url),legacy=require('../../../app/core/simpack/simpack.js') as{createSimPack(input:Record<string,unknown>):unknown;packSimPack(project:unknown):Uint8Array;unpackSimPack(bytes:Uint8Array):{manifest:{title:string};files:Record<string,Uint8Array>}};
describe('legacy compatibility',()=>{it('opens archives produced by legacy codec',()=>{const old=legacy.createSimPack({id:'old',title:'Old',sourceBytes:new TextEncoder().encode('source'),fileName:'x.bin',schema:{meta:{id:'old'}}});const restored=unpackSimPack(legacy.packSimPack(old));expect(restored.manifest.title).toBe('Old');});it('legacy codec opens archives produced by TypeScript codec',()=>{const next=createSimPack({id:'next',title:'Next',sourceBytes:new TextEncoder().encode('source'),fileName:'x.bin',schema:{meta:{id:'next'}}});expect(legacy.unpackSimPack(packSimPack(next)).manifest.title).toBe('Next');});});
