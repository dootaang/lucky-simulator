<script lang="ts">import type { Snippet } from 'svelte'; let { children, variant='primary', disabled=false, onclick }: { children:Snippet; variant?:'primary'|'secondary'|'danger'|'ghost'; disabled?:boolean; onclick?:(event:MouseEvent)=>void }=$props();</script>
<button class:primary={variant==='primary'} class:danger={variant==='danger'} class:ghost={variant==='ghost'} {disabled} {onclick}><span>{@render children()}</span></button>
<style>button{min-height:2.6rem;padding:0 var(--space-4);border:1px solid var(--color-line);border-radius:var(--radius-md);background:var(--color-surface-raised);color:var(--color-text);cursor:pointer;transition:transform var(--motion-fast),border-color var(--motion-fast)}button:hover:not(:disabled){border-color:var(--color-accent);transform:translateY(-1px)}button:focus-visible{outline:2px solid var(--color-accent);outline-offset:2px}button.primary{background:var(--color-accent);color:#211508;border-color:var(--color-accent);font-weight:700}button.danger{border-color:var(--color-danger);color:var(--color-danger)}button.ghost{background:transparent;border-color:transparent}button:disabled{opacity:.45;cursor:not-allowed}</style>
