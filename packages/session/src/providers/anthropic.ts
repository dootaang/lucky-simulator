import type { ModelProvider } from '../index.ts';import { jsonText,maskSecrets } from './openai.ts';
export interface AnthropicOptions{apiKey:string;model:string;endpoint?:string;temperature?:number;maxTokens?:number;fetch?:typeof globalThis.fetch;}
function promptParts(messages:Array<{role:string;content:string}>){const system=messages.filter(m=>m.role==='system').map(m=>m.content).join('\n\n'),chat:Array<{role:'user'|'assistant';content:string}>=[];for(const item of messages.filter(m=>m.role!=='system')){const role=item.role==='assistant'?'assistant':'user';const last=chat.at(-1);if(last?.role===role)last.content+=`\n\n${item.content}`;else chat.push({role,content:item.content});}return{system,chat};}
export function createAnthropicProvider(options:AnthropicOptions):ModelProvider{const request=options.fetch??globalThis.fetch;return{async complete({prompt,signal,format='json'}){const {system,chat}=promptParts(prompt.messages),response=await request(options.endpoint??'https://api.anthropic.com/v1/messages',{method:'POST',headers:{'content-type':'application/json','x-api-key':options.apiKey,'anthropic-version':'2023-06-01','anthropic-dangerous-direct-browser-access':'true'},body:JSON.stringify({model:options.model,max_tokens:options.maxTokens??4096,temperature:options.temperature??.8,...(system?{system}:{}),messages:chat}),...(signal?{signal}:{})});if(!response.ok)throw new Error(`model_http_${response.status}:${maskSecrets(await response.text().catch(()=>''),options.apiKey).slice(0,300)}`);const body=await response.json()as {content?:Array<{type?:string;text?:string}>},text=(body.content??[]).filter(x=>x.type==='text').map(x=>x.text??'').join('');if(!text)throw new Error('model_content_missing');return format==='prose'?{text,events:[],speakers:[],memories:[]}:jsonText(text);}};}
export {promptParts as anthropicPromptParts};
