import{describe,expect,it}from'vitest';import{exportRisuPersonaPng,importRisuPersonaPng}from'../src/persona-png.ts';import type{Persona}from'../../risu/src/contracts.ts';
const png=Uint8Array.from(Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=','base64'));const persona:Persona={contract:'persona/0.1',id:'persona-owner',name:'주인장',prompt:'나는 오래된 여관의 주인장이다.',icon:'',note:'테스트',embeddedModule:null,source:null,version:1};
describe('persona PNG',()=>{it('exports and imports the same persona fields',()=>{const restored=importRisuPersonaPng(exportRisuPersonaPng(persona,png),'owner.png');expect(restored).toMatchObject({...persona,icon:expect.stringMatching(/^data:image\/png;base64,/),source:expect.objectContaining({path:'tEXt:persona'})});});it('reports a broken PNG clearly',()=>{expect(()=>importRisuPersonaPng(Uint8Array.of(1,2,3))).toThrow(/persona_png_invalid/);});it('reports missing tEXt metadata clearly',()=>{expect(()=>importRisuPersonaPng(png)).toThrow('persona_png_metadata_missing');});});
